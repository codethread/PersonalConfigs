# Machine-specific hardware configuration — NOT committed with real values.
# Regenerate on the target machine after NixOS install:
#
#   sudo nixos-generate-config --show-hardware-config > nix/hosts/vm/hardware-configuration.nix
#
{ ... }: { }
