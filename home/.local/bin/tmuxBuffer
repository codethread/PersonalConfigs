#!/bin/bash
# :module: tmux clipboard and buffer management utility

# $HOME/bin/vim-edit-tmux-output

file=$(mktemp).sh
tmux capture-pane -pS -32768 >"$file"
tmux new-window -n:history "$EDITOR '+ normal G $' $file"
