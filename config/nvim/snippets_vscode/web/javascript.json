{
  "Console log": {
    "prefix": "clc",
    "body": ["console.log('codethread', $0)"],
    "description": "create console log for easy filtering"
  },
  "Console info": {
    "prefix": "cli",
    "body": ["console.info('codethread', $0)"],
    "description": "create console log for easy filtering"
  },
  "Console warn": {
    "prefix": "clw",
    "body": ["console.warn('codethread', $0)"],
    "description": "create console log for easy filtering"
  },
  "Console debug": {
    "prefix": "cld",
    "body": ["console.debug('codethread', $0)"],
    "description": "create console log for easy filtering"
  },
  "comment block": {
    "prefix": "cbb",
    "body": [
      "//*****************************************************************************//",
      "// $0",
      "//*****************************************************************************//"
    ]
  },
  "styled component": {
    "prefix": "st",
    "body": ["\\${({ theme }) => theme$1}"],
    "description": "Insert embeded theme in styled component"
  },
  "useEffect": {
    "prefix": "ue",
    "body": ["React.useEffect(()=>{ $0; return ()=> { $1 }},[$2])"]
  },
  "useEffect log unmount": {
    "prefix": "uel",
    "body": [
      "React.useEffect(()=>{ console.log('codethread mount $1'); return ()=> { console.log('codethread unmount $1') }},[])"
    ]
  }
}
