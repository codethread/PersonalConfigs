# Enable mouse mode (tmux 2.1 and above)
set -g mouse on
set -g base-index 1
set -g pane-base-index 1

#tmux copy and paste
# setw -g mode-keys vi
# bind P paste-buffer

# bind-key -t vi-copy 'v' begin-selection
# bind-key -t vi-copy 'y' copy-selection
# bind-key -t vi-copy 'r' rectangle-toggle

# remap prefix from 'C-b' to 'C-a'
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

# split panes using | and -
bind % split-window -h -c "#{pane_current_path}"
bind '"' split-window -c "#{pane_current_path}"
bind - split-window -c "#{pane_current_path}"
bind | split-window -h -c "#{pane_current_path}"

# bind | split-window -h '#{pane_current_path}'
# bind - split-window -v '#{pane_current_path}'
unbind '"'
unbind %

# reload config file (change file location to your the tmux.conf you want to use)
bind r source ~/.tmux.conf

# # Use Alt-vim keys without prefix key to switch panes
bind -r M-h select-pane -L
bind -r M-j select-pane -D 
bind -r M-k select-pane -U
bind -r M-l select-pane -R

# switch panes using Alt-arrow without prefix
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

set-window-option -g mode-keys vi
bind-key -T copy-mode-vi 'v' send -X begin-selection
bind-key -T copy-mode-vi 'y' send -X copy-selection-and-cancel

######################
### DESIGN CHANGES ###
######################

# panes
set -g pane-border-fg black
set -g pane-active-border-fg brightred

## Status bar design
# messaging
set -g message-fg black
set -g message-bg yellow
set -g message-command-fg blue
set -g message-command-bg black

#window mode
# setw -g mode-bg colour6
setw -g mode-bg colour6
setw -g mode-fg {{WHITE}}

# status line
set -g status-justify left
set -g status-bg default
set -g status-fg colour12
set -g status-interval 2

# window status
setw -g window-status-format " #F#I:#W#F "
setw -g window-status-current-format " #F#I:#W#F "
setw -g window-status-format "#[fg=magenta]#[bg=black] #I #[bg=cyan]#[fg=colour8] #W "
setw -g window-status-current-format "#[bg=brightmagenta]#[fg=colour8] #I #[fg=colour8]#[bg=colour14] #W "
setw -g window-status-current-bg {{WHITE}}
setw -g window-status-current-fg colour11
setw -g window-status-current-attr dim
setw -g window-status-bg green
setw -g window-status-fg black
setw -g window-status-attr reverse

set -g default-terminal "screen-256color"

# The modes {
        setw -g clock-mode-colour colour135
        setw -g mode-attr bold
        setw -g mode-fg colour196
        setw -g mode-bg {{BASE}}
# }

# The panes {
        set -g pane-border-bg {{BACKGROUND}}
        set -g pane-border-fg {{BASE}}
        set -g pane-active-border-bg {{BACKGROUND}}
        set -g pane-active-border-fg {{PRIME_COLOR}}

# }
# The statusbar {

        set -g status-position bottom
        set -g status-bg {{BACKGROUND}}
# set -g status-attr dim
        set -g status-left '#[bg={{SECONDARY}},fg={{TEXT_CONTRAST}},bold]#{?client_prefix,#[bg={{CONTRAST}}],} #S #[bg={{BACKGROUND}}]#[fg={{SECONDARY}}]#{?client_prefix,#[fg={{CONTRAST}}],} '
        set -g status-right '#[bold]#[bg={{BACKGROUND}},fg={{BASE}}]#[bg={{BASE}}]#{battery_status_fg} ♥ #{battery_percentage} ⮃ #{cpu_fg_color}#{cpu_bg_color}☀#{cpu_percentage} #[bg={{BASE}}]#[fg={{SECONDARY}}]#{?client_prefix,#[fg={{CONTRAST}}],}#[bg={{SECONDARY}},fg={{TEXT_CONTRAST}},bold]#{?client_prefix,#[bg={{CONTRAST}}],} %H:%M:%S ⮃ %A %d/%b  '
# set -g status-right-length 52
# set -g status-left-length 110

        set -g window-status-current-format "#[bg={{BACKGROUND}},fg={{PRIME_COLOR}}]#{?client_prefix,#[fg={{CONTRAST}}],} #[fg={{PRIME_COLOR}},bg={{BACKGROUND}},bold]#{?client_prefix,#[fg={{CONTRAST}}],}#I | #W #[bg={{PRIME_COLOR}},fg={{BACKGROUND}}]#{?client_prefix,#[bg={{CONTRAST}}],}"
        set -g window-status-format "#[bg={{BACKGROUND}},fg={{BASE}}] #[fg={{BASE}},bg={{SECONDARY}},bold]#I | #W #[bg={{BASE}},fg={{BACKGROUND}}]"

        setw -g window-status-bell-attr bold
        setw -g window-status-bell-fg colour255
        setw -g window-status-bell-bg colour1
# }
# The messages {
        set -g message-attr bold
        set -g message-fg colour232
        set -g message-bg colour166
# }
#  CPU plugin {

        set -g @cpu_low_fg_color "#[fg={{SECONDARY}}]"
        set -g @cpu_medium_fg_color "#[fg={{yellow}}]"
        set -g @cpu_high_fg_color "#[fg={red}]"

        set -g @cpu_low_bg_color "#[bg={{BASE}}]"
        set -g @cpu_medium_bg_color "#[bg={{BASE}}]"
        set -g @cpu_high_bg_color "#[bg={{BASE}}]"

        set -g @batt_color_full_charge "#[fg={{SECONDARY}}]"
        set -g @batt_color_high_charge "#[fg={{SECONDARY}}]"
        set -g @batt_color_medium_charge "#[fg=yellow]"
        set -g @batt_color_low_charge "#[fg=red]"
# }

# List of plugins
        set -g @plugin 'tmux-plugins/tpm'
        set -g @plugin 'tmux-plugins/tmux-sensible'
        set -g @plugin 'tmux-plugins/tmux-cpu'
        set -g @plugin 'tmux-plugins/tmux-battery'

# Other examples:
# set -g @plugin 'github_username/plugin_name'
# set -g @plugin 'git@github.com/user/plugin'
# set -g @plugin 'git@bitbucket.com/user/plugin'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
                run '~/.tmux/plugins/tpm/tpm'
