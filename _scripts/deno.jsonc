{
  "tasks": {
    "dev": "deno run --watch --allow-env ./tmp.ts",
    "s": "deno run --allow-env --allow-read ./getSlackCreds.ts",
  },
  // don't put imports in here or it breaks remote execution
}
